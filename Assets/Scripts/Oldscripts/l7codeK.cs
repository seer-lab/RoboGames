using UnityEngine;
using System.Collections;

public class l7codeK : MonoBehaviour {

	// Use this for initialization
	void Start () {
		this.GetComponent<TextMesh> ().text = "part" +
			"\n" +
			"\n" +
			"\n" + 
			"\n\tint " + 
			"\n\t             Math.abs" +
			"\n\t             Math.abs" +
			"\n\t             Math.abs" +
			"\n\t             Math.abs" +
			"\n\t             Math.abs" +
			"\n\t             Math.abs" +
			"\n\tif                index                   return compare" +
			"\n\telse if                          return" +
			"\n\telse   return ";
		this.GetComponent<TextMesh> ().color = Color.blue;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
